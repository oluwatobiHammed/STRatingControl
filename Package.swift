// swift-tools-version:5.5

// swift-tools-version:5.5

import PackageDescription

let package = Package(
    name: "_Pods",
    products: [
        .library(
            name: "_Pods",
            targets: ["_Pods"]
        ),
    ],
    targets: [
        .target(
            name: "_Pods",
            dependencies: [])
    ]
)
